// // import { isElectron } from '../../constants';
// import { isElectron } from '../../constants';
//
// function loadModelsWeb() {
//   // collect models at compile time
//   const ctx = require.context('@/../../static/mmci-cli/models', true, /^\.\/([^/]*)\/\1\.json$/);
//   return ctx.keys().map(key => ctx(key));
// }

const models = [];
export default models;
