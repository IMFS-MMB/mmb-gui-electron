{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "rule",
  "type": "object",
  "required": [
    "name",
    "description",
    "coefficients"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "name": {
      "description": "Name of the rule. Displayed in the GUI and used to specify rule compatibility in models' .json files",
      "type": "string"
    },
    "coefficients": {
      "$ref": "common#/definitions/arrayOfCoefficients"
    },
    "description": {
      "$ref": "common#/definitions/description"
    }
  }
}
